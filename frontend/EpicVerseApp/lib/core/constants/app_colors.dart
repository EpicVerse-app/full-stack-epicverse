import 'package:flutter/material.dart';

class AppColors {
  // EpicVerse Brand Palette
  static const Color primaryGold = Color(0xFFC5A358); // Rich Metallic Gold
  static const Color accentGold = Color(0xFFF3D081);  // Highlight Gold
  static const Color deepGold = Color(0xFF86632B);    // Shadow Gold
  
  // Backgrounds
  // Deep space/purple mystery matching the image
  static const Color background = Color(0xFF1B072D); // Darker, more reddish purple
  static const Color surface = Color(0xFF2E134D);   // Lighter purple for cards
  static const Color surfaceElevated = Color(0xFF41246D); // Button primary color
  
  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFAD93B0); // Subdued text matching image
  static const Color textMuted = Color(0xFF6B5875);    // Lower text
  
  // States
  static const Color profileActive = Color(0xFF4ADE80);
  static const Color error = Color(0xFFEF4444);
  
  // Glassmorphism for EpicVerse
  static const Color glassBackground = Color(0x07FFFFFF);
  static const Color glassBorder = Color(0x26C5A358);
  static const Color glassShadow = Color(0x80000000);
}
